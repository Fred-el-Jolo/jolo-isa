#!/usr/bin/env python3
"""Launcher: finds the `isa` package next to this file (runtime/bin/../isa) and runs its CLI."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
from isa.cli import main  # noqa: E402

sys.exit(main())
